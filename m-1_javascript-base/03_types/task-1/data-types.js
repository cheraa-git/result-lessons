const name = 'Alexandr'
console.log(name, typeof name)

const age = 20
console.log(age, typeof age)

const isMarried = false
console.log(isMarried, typeof isMarried)

const favoriteCar = {
  model: 'Tesla',
  acceleration: 3
}
console.log(favoriteCar, typeof favoriteCar)

const currentCar = undefined
console.log(currentCar, typeof currentCar)

const currentJob = null
console.log(currentJob, typeof currentJob)

const id = Symbol('myId')
console.log(id, typeof id)

const currentLesson = 3n
console.log(currentLesson, typeof currentLesson)